#include <iostream>
#include <cmath>

int main(){
    /* char x = 'C';
    bool hard = false;
    std::cout<< "The value in the varibale x is " << x << std::endl;
    
    x = 'A';
    std::cout<< "The new value of x is "<< x << '\n';
    std::cout<< "The size of char data type is " << sizeof(long) << " bytes" <<std::endl;

    long y = pow(2,sizeof(long)*4);
    std::cout<< "Available number of long: " << y; */
    
    char c = 99;
    std::cout<<c;
    // use ctrl + alt + n to run code
}