#include <iostream>

int main(void) {
    // const int n = 5;
    // int arr[n] = {10,20,30,40,50};
    // n++; error, can't modify a read-only obj (n is now immutable obj)

    // const int n; // error, no initializer
    const int m = 123; // ok
}